apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: pipeline

resources:
  - helmrepository-bitnami.yaml
  - helmrelease-postgresql.yaml
  - helmrelease-kafka.yaml
  - postgresql-secret.enc.yaml
